/**
 * @class
 * @classdesc Containing method for the contact form.
 *
 * @method sendMessage
 * Sends the message
 *
 * @medthod gatherMessage
 * gathers the message and places it into the correct format.
 */

 export function Contact() {
   const form = document.querySelector('.contact-form');

   this.sendMessage = () => {

   };

   this.gatherMessage = () => {

   };
 }