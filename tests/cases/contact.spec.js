import assert from 'assert';
import chai from 'chai';
// import { Contact } from '@js/components/menu-open';

chai.use(require('chai-dom'));

var
  expect = chai.expect;

before (function(){
  // message body
  // from who
  // title
});

describe('', function() {
  it('gather all of the info into json', function() {
   
  });

  it('send the gathered info to the server and get a 200', function() {
    
  });

  it('send the wrong gathered info to the server and get a 500', function() {
    
  });
});

after (function(){
  
});